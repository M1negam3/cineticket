<div id="app">
    <router-view></router-view>
  </div>
  
  <!-- Vue.js CDN -->
  <script src="https://cdn.jsdelivr.net/npm/vue@2"></script>
  
  <!-- VueRouter CDN (Version 3) -->
  <script src="https://cdn.jsdelivr.net/npm/vue-router@3"></script>
  
  <script type="module">
    // Import Ihrer Vue-Komponenten
    import MealReport from '/vue/meal-report.js';
  
    const routes = [
      { path: '/meal/report', component: MealReport },
    ];
  
    // Erstellen Sie den Router
    const router = VueRouter.createRouter({
      history: VueRouter.createWebHashHistory(),
      routes,
    });
  
    // Erstellen Sie die Vue-App und binden Sie den Router ein
    const app = Vue.createApp({});
    app.use(router);
    app.mount('#app');
  </script>