<div id="app" class="content-container p-5">
  <router-view></router-view>
</div>

<script type="module">

  import SeatReport from '/vue/seat-report.js';
  import SeatConfirmation from '/vue/seat-confirmation.js'

  const routes = [
    { path: '/', redirect: '/seat/report' },
    { path: '/seat/report', component: SeatReport },
    { path: '/seat/confirmation', component: SeatConfirmation }
  ];


  const router = VueRouter.createRouter({
    history: VueRouter.createWebHashHistory(),
    routes,
  });


  const app = Vue.createApp({});
  app.use(router);
  app.mount('#app');
</script>
